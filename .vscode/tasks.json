{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build pos_admin_btp",
      "type": "shell",
      "command": "mvn clean install",
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}